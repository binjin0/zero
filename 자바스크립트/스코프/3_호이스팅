/*
호이스팅 : 변수선언/함수선언을 해당 스코프의 최상단으로 끌어올려지는 현상

이러한 호이스팅을 해결하기 위해서는 var 대신 let을 사용하면 된다.
그러나 let,const도 호이스팅은 일어나지만 내부적으로 보이지 않는 것처럼 하여 일시적 사각 지대에 의해 가려지고 에러를 출력된다. 

*/
function foo() {
  console.log(hoist); //아직 hosit를 선언하지 않았음에도 오류가 일어나지 않음 -> why? 호이스팅 때문에. var hoist 선언이 스코프(범위)안에서 최상단으로 끌어올려졌기때문에 이미 선언된 것으로 간주되어서!
  var hoist = "호이스팅";
  console.log(hoist);
}

foo();
